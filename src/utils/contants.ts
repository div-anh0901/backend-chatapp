export enum Routes {
    AUTH = 'auth',
    USERS = 'users',
    CONVERSATIONS= "conversations"
  }
  
  export enum Services {
    AUTH = 'AUTH_SERVICE',
    USERS = 'USERS_SERVICE',
    CONVERSATIONS = "CONSERVATION_SERVICE"
  }